sources: 
  arxiv: 
    enabled: true
    categories: ["cs.AI", "cs.CY"]
    # max_papers: 1000
    max_papers: 10
    
  pubmed:
    enabled: true
    max_papers: 1000
  
storage:
  raw_pdf_dir: data/raw/pdfs
  metadata_pdf_dir: data/raw/metadata